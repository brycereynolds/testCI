dependencies:
  override:
    - git clone https://github.com/ansible/tower-cli.git ../tower-cli
    - make install:
        pwd: ../tower-cli
deployment:
  prod:
    branch: master
    commands:
      - bash -x deploy.sh